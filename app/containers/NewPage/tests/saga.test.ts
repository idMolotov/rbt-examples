/**
 * Test sagas
 */

// import { take, call, put, select } from 'redux-saga/effects';
// import newPageSaga from '../saga';

// const generator = newPageSaga();

describe('newPageSaga Saga', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
