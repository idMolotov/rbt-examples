/*
 * SecondPage Messages
 *
 * This contains all the text for the SecondPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SecondPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SecondPage container!',
  },
});
